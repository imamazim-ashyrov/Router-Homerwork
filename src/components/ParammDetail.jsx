import React from 'react'
import { useParams } from 'react-router-dom'

const ParammDetail = () => {
    const param = useParams()
    console.log(param);
  return (
    <div>
      
    </div>
  )
}

export default ParammDetail